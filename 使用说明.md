# 华硕天选一代 FA506IV —— 黑苹果 EFI 与安装说明

为你的机器（ASUS TUF Gaming A15 FA506IV / Ryzen 7 4800H + RTX 2060 + 16GB）定制
生成时间：2026-09-12 | 引导：OpenCore 1.0.7 RELEASE | 目标系统：macOS Sonoma 14.x

---

## 一、你的硬件在 macOS 下的支持情况

| 部件 | 型号 | macOS 情况 |
| --- | --- | --- |
| CPU | Ryzen 7 4800H（8 核 16 线程） | 可用，需 AMD 内核补丁 + `ProvideCurrentCpuInfo`（已配好） |
| 核显 | Radeon Vega（Renoir） | 可用，由 NootedRed 驱动，带硬件加速 |
| 独显 | NVIDIA RTX 2060 | **无驱动，永久不可用**（Turing 架构没有 macOS 驱动） |
| 有线网卡 | Realtek RTL8168/8111 | 可用（RealtekRTL8111），已设为内建，供 iCloud 使用 |
| 无线网卡 | Intel AX210 (Wi-Fi 6E) | 可用（AirportItlwm），**6GHz 频段不可用**，2.4/5G 正常 |
| 蓝牙 | Intel AX210 | 可用（IntelBluetoothFirmware + BlueToolFixup） |
| 声卡 | Realtek ALC256 | 可用（AppleALC，layout-id 23） |
| 触控板 | I2C HID | 基本可用（VoodooI2C），多指手势可能不全 |
| 键盘 | PS/2 | 可用，Fn 亮度/音量键可用（BrightnessKeys） |
| 无线网卡原装 | Realtek RTL8822CE | 无驱动，已被 AX210 取代 |
| 硬盘 | KIOXIA NVMe | 可用，附 NVMeFix 改善电源管理 |

---

## 二、这个文件夹里有什么

```
ASUS-TUF-A15-FA506IV/
├─ EFI-Final/EFI/          ← 装好系统后日常使用（NootedRed 核显加速）
├─ EFI-Install/EFI/        ← 安装 / 大版本升级 macOS 时使用（WhateverGreen 代替 NootedRed）
├─ com.apple.recovery.boot/ ← macOS Sonoma 恢复镜像（BaseSystem.dmg，753MB，已校验）
├─ 可选驱动/itlwm.kext      ← 只有装 macOS 15 (Sequoia) 时才需要
└─ 使用说明.md
```

两个 EFI 的差别只有一处：`Kernel → Add` 里 `NootedRed.kext` 与 `WhateverGreen.kext` 的开关。
原因是同型号的社区记录和 NootedRed 作者都提到：**安装阶段用 NootedRed 容易黑屏**，所以先用
WhateverGreen 把系统装进硬盘，装完再换成 EFI-Final 启用核显加速。两边都准备好了，装的时候
哪个能亮就用哪个。

EFI 已通过 OpenCore 官方校验工具 `ocvalidate`（1.0.7 版）：**No issues found**。

---

## 三、装机前必做的准备

1. **备份数据**。如果 Windows 开了 BitLocker，先在「控制面板 → 管理 BitLocker」里**暂停保护**，
   否则改启动顺序会让 Windows 要求恢复密钥。
2. 准备一个 **≥16GB 的 U 盘**（做安装盘，里面数据会被清空）。
3. 想好 macOS 装在哪：
   - **有第二块硬盘**（内置或 USB 硬盘盒）：最省事，整块盘给 macOS，互不干扰，推荐。
   - **只有一块硬盘**：先在 Windows「磁盘管理」里把 D 盘压缩出 **120GB 以上的未分配空间**，
     安装时用「磁盘工具」把那块未分配空间抹成 APFS（不要动 Windows 分区）。
4. 进 BIOS（开机连按 **F2**）检查：
   - `Secure Boot` → Disabled（你的机器现在已经是关闭状态）
   - `Fast Boot` → Disabled
   - 显存：华硕 BIOS 不暴露 `UMA Frame Buffer Size` 这项，**需要用 Smokeless_UMAF 改成 4G**
     （NootedRed 要求至少 512MB；改成 4G 后核显明显更流畅），做法见 README 的
     「把核显显存（UMA Frame Buffer）改成 4GB」一节

---

## 四、制作 macOS 安装 U 盘（Windows 下，不需要 Mac）

恢复镜像我已经帮你下好了，放在 `com.apple.recovery.boot` 文件夹里（753MB）。

1. 把 U 盘格式化成 **FAT32**（容量大于 32GB 的用 [Rufus](https://rufus.ie/) 选「Large FAT32」）。
2. 把整个 `com.apple.recovery.boot` 文件夹拷到 U 盘**根目录**。
3. 把 **EFI-Install 里的 `EFI` 文件夹**整个拷到 U 盘**根目录**（和 `com.apple.recovery.boot` 并排）。
4. 拷完之后 U 盘根目录应该长这样：

```
U盘根目录/
├─ EFI/
│   ├─ BOOT/BOOTx64.efi
│   └─ OC/  (config.plist、Kexts、ACPI、Drivers)
└─ com.apple.recovery.boot/
    ├─ BaseSystem.dmg
    └─ BaseSystem.chunklist
```

> 注意：`BaseSystem.dmg` 是完整文件，别用 7-Zip 打开或解压它。

---

## 五、安装 macOS

1. 插上 U 盘，开机连按 **Esc**（或 F8）调出启动菜单，选 **UEFI: 你的U盘**。
2. 进入 OpenCore 菜单（黑底图标界面），选 **macOS Base System**。
3. 进入安装界面 → 先打开「**磁盘工具**」：
   - 左上角「显示」→「显示所有设备」
   - 选中你要装的**整块硬盘或未分配空间** → 抹掉 → 格式选 **APFS**，方案 **GUID 分区图**
4. 退出磁盘工具 → 选「安装 macOS Sonoma」→ 按提示走（会联网下载完整系统，需要 Wi-Fi 或用有线网）。
5. 安装过程中会重启 3~4 次，**每次都要重新从 U 盘启动**，并且在 OpenCore 菜单里选
   「macOS Installer」或「macOS 所在硬盘」那一项继续，直到进入桌面设置向导。

> 如果安装界面黑屏、卡住或重启循环：把 U 盘里的 `EFI` 换成 **EFI-Final** 里的 `EFI` 再试一次。
> 两个版本都试过还不行，请把屏幕上的报错拍照发我（见第九节排错表）。

---

## 六、装好之后要做的三件事

**1. 把 EFI 换到硬盘上，让机器不插 U 盘也能进系统**

把 **EFI-Final 里的 `EFI` 文件夹**拷到 macOS 所在硬盘的 EFI 分区（EFI 分区在 Windows 里看不到，
可以用 DiskGenius / EasyUEFI 挂载，或者装完 macOS 后在 macOS 里操作）。如果分区里已有 `EFI` 文件夹，
先删除旧的再拷新的。

**2. 添加启动项（三选一）**

| 方式 | 做法 | 优缺点 |
| --- | --- | --- |
| A 手动 | 开机按 Esc 选 OpenCore | 最简单，但每次都要按键 |
| B EasyUEFI | 用 EasyUEFI 新建启动项，指向 `\EFI\OC\OpenCore.efi`，命名为 OpenCore 并排到第一位 | 图形界面，直观 |
| C bcdedit | 管理员 CMD 执行：`bcdedit /set "{bootmgr}" path \EFI\OC\OpenCore.efi` | 让原来的「Windows Boot Manager」变成 OpenCore 菜单，里面还能选 Windows |

方式 C 的还原命令（想回到直接进 Windows 时用）：

```
bcdedit /set "{bootmgr}" path \EFI\Microsoft\Boot\bootmgfw.efi
```

**3. 稳定后去掉调试参数（可选）**

用 ProperTree 或任何文本编辑器打开 `EFI/OC/config.plist`，找到 `NVRAM → Add → 7C436110-...→ boot-args`，
把

```
-v debug=0x100 keepsyms=1 alcid=23 npci=0x2000 revblock=media -NRedDPDelay
```

改成

```
alcid=23 npci=0x2000 revblock=media
```

（`-v` 是啰嗦模式、`debug=0x100 keepsyms=1` 是调试用，`-NRedDPDelay` 是显示延迟补丁，
显示正常后可以删掉；`alcid=23` 是声卡，`npci=0x2000` 和 `revblock=media` 保留）

---

## 七、怎么判断"装好了"

- 关于本机 → 系统报告 → 图形卡：显示 **AMD Radeon Vega** 且有显存（不是「显示器 8MB」）
- 菜单栏出现 Wi-Fi 图标，能搜到家里的 2.4/5G 网络
- 声音、亮度 Fn 键、音量 Fn 键、触控板、电池图标、摄像头都正常

---

## 八、这套配置的已知限制

1. **RTX 2060 永远用不了**。视频输出方面：HDMI 走核显可以用，Type-C/DP 走独显所以不行。
2. **独显会一直通电**（没驱动，无法休眠），续航会比 Windows 短，机器也可能略热。
3. **睡眠**：同型号记录是「能睡，偶尔立即唤醒」。需要的话我可以加 `SSDT-GPRW` 深度修复。
4. **触控板**：AMD 平台的 I2C 触控板支持一般，移动/点击没问题，多指手势可能不全。
5. **Wi-Fi 6GHz 不可用**（AX210 的 6E 只能当 2.4/5G 用）。
6. **NootedRed 官方声明**：Sonoma 上有偶发卡顿/崩溃是已知问题；建议桌面用静态壁纸、
   头像不要用 Memoji，Chromium 浏览器如遇花屏可在 `chrome://flags` 关掉 GPU Rasterisation。
7. **iCloud/iMessage**：这套 EFI 已经生成好一套三码（MacBookPro16,2）。
   想用 Apple 服务时，建议先插网线登录 Apple ID（有线网卡已设为内建）。
   如果你希望换成自己的序列号，可以用 GenSMBIOS 重新生成后替换
   `PlatformInfo → Generic` 的 SystemSerialNumber / MLB / SystemUuid / ROM 四项。

---

## 九、排错对照表

| 现象 | 处理办法 |
| --- | --- |
| 卡在 `EB|LOG:EXITBS:START` | `Booter → Quirks`：`RebuildAppleMemoryMap` 改 **False**、`EnableWriteUnprotector` 改 **True**，重试 |
| 开机黑屏、直接进 UEFI、无任何输出 | `Booter → Quirks`：`AvoidRuntimeDefrag` 改 **True**（这是 Dortania 官方标准值）再试 |
| 卡在 `OCB: StartImage failed` | EFI 没拷全，检查 U 盘 EFI/OC/Kexts 里是否有 26 个 .kext 文件夹 |
| 菜单里只有 `no name` 和 `Windows`，找不到安装项 | `no name` 是 U 盘这个卷本身，选它等于让 OpenCore 重启自己（日志里会写 `Failed to start OpenCore image - Already started`）；真安装项叫 `macOS Base System (dmg)`，本项目已把 `HideAuxiliary` 设为 false 让它默认显示，旧配置需按 **空格键** 显示 |
| 苹果 logo 卡一半 | boot-args 里保留 `-v`，看最后一行报错并拍照发我 |
| 安装器进不去、转圈后重启 | 换另一个 EFI（EFI-Install ↔ EFI-Final） |
| 没声音 | 依次试 boot-args 里 `alcid=11`、`13`、`21`、`23`、`56` |
| 重启几次后内置扬声器无声、耳机正常 | 这是 ALC256 热重启后功放初始化没跑的问题：在 boot-args 里加 `alcdelay=1500`（`修复配置/5-重启后扬声器无声-加alcdelay/` 已带），应急可先**关机再开机**（不要用重启） |
| Wi-Fi 搜不到 | 确认 macOS 是 14.4 以上；若装的是 14.0–14.3，需要换成对应 Sonoma14.0 版的 AirportItlwm |
| 某个 USB 口不认设备 | `Kernel → Add` 里把 `GenericUSBXHCI.kext` 的 Enabled 改成 True |
| 进不去 Windows 了 | 管理员 CMD：`bcdedit /set "{bootmgr}" path \EFI\Microsoft\Boot\bootmgfw.efi` 后重启 |
| 要看引导日志 | 已开启（`Misc → Debug → Target = 0x43`），日志文件写在 EFI 分区里，发我即可 |

改 config.plist 建议用 [ProperTree](https://github.com/corpnewt/ProperTree)（免费、跨平台），
不要用 OpenCore Configurator 之类的工具，容易写坏文件。

---

## 十、来源与说明

- OpenCore 1.0.7、Lilu 1.7.2、VirtualSMC 1.3.7、AppleALC 1.9.7、WhateverGreen 1.7.0、
  RestrictEvents 1.1.6、NVMeFix 1.1.3、VoodooPS2 2.3.7、VoodooI2C 2.9.1：acidanthera / VoodooI2C 官方发布
- 核显驱动 NootedRed 0.8.10：ChefKissInc 官方发布（作者声明其代码不允许被 AI 分析/训练，
  这里只是原样使用官方发布的二进制）
- AMD 内核补丁：AMD-OSX/AMD_Vanilla（已按 4800H 的 8 核修改 cpuid 常量）
- ACPI 补丁（SSDT-EC / HPET / PLUG-ALT / PNLF / USBX / XOSI）、USB 端口映射（UTBMap）：
  参考同型号开源项目 Lyianu/FA506IV-OpenCore，并按 OpenCore 1.0.7 重新整理
- 无线/蓝牙：OpenIntelWireless（itlwm 2.3.0、IntelBluetoothFirmware 2.4.0）
- 有线网卡：Mieze/RTL8111_driver_for_OS_X 3.0.0
- 显存解锁（UMA Frame Buffer Size）：DavidS95/Smokeless_UMAF（原项目 SmokelessCPU）
- 提醒：苹果 EULA 只允许 macOS 装在苹果硬件上，自己折腾属于灰区，别用于商业用途

---

## 十一、接下来我可以继续帮你

1. 告诉我**磁盘情况**（几块物理硬盘、D 盘是内置还是 USB 外接），我给你具体的分区/安装方案。
2. U 盘做好后开机遇到任何报错，把**屏幕照片**或 EFI 分区里的**日志文件**发我，我改配置重新打包。
3. 装好后想要更省电（关独显）、更完美的睡眠，我可以给你加对应的 SSDT。
